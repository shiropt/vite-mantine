import { Box } from '@mantine/core';
import { Login as LoginComponent } from '.';

export default {
  title: 'organisms/Login',
};

export const Login = () => (
  <Box>
    <LoginComponent />
  </Box>
);
